# string-handling-
loops and string manipulation

statement = "Good morning" 
final_string = ""
for i in statementt :
for i in range(len(statement)) :

   if 1 % 2 ==  1 : 
   final string = statement[i].upper()
else :
    final_string += statement[i].lower()

print(final_string)

'''
string handling is done via loops and string manipulation 
loops allow us to handle strings since strings are statements
strings can be manipulated via indexing, split, strip and replace techniques in string manipulation
remember len and range allow us to index strings in string manipulation
if and else provide conditions for a statement (strings)
'''
Tendani Maumela 
